# Copy this file to ".env" (without .example) and adjust the values if needed.
# ".env" is never committed -- it's already excluded in .gitignore.
POSTGRES_DB=covid_regions
POSTGRES_USER=covid_app
POSTGRES_PASSWORD=change_me
